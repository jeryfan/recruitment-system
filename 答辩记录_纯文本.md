1.你提到系统采用了基于 TF-IDF 的智能岗位推荐算法，请简要说明 TF-IDF 的基本原理，以及在本系统中是如何计算简历与职位之间相似度的？
答：TF-IDF 是一种常用的文本特征提取方法，TF 是词频，IDF 是逆文档频率。在本系统中，我是先将所有职位描述和简历内容分词，然后分别计算每个词的 TF-IDF 值，生成职位向量和简历向量，最后通过余弦相似度计算两者之间的相似度，返回相似度最高的前 10 个职位推荐给用户。（回答正确）

2.系统权限管理是基于 lin-cms 框架实现的，请说明一下系统中是如何区分求职者、HR 和管理员三类角色的权限的，如果 HR 试图调用管理员接口会发生什么？
答：系统是通过 JWT Token 来区分角色的，每个用户登录后会拿到一个 token，前端请求时带上这个 token，后端解析 token 就知道是谁了。如果 HR 调用管理员接口，后端会返回 401 未授权错误。（基本正确）

3.系统前端使用了 Vue 3 + Vite 的求职者端和 Vue 2 的管理端两个版本，你为什么没有统一使用 Vue 3？这样做会不会增加维护成本？
答：因为我先写管理端的时候还不会 Vue 3，所以用了 Vue 2，后来做求职者端的时候学了 Vue 3，觉得 Vue 3 更好用就用了 Vue 3。这样做确实会增加维护成本，但两个端功能不一样，分开维护也可以接受。（错误）

4.请描述一下系统的 Docker Compose 部署架构，如果 MySQL 容器因异常重启，后端服务会如何处理？
答：系统通过 Docker Compose 部署了四个服务：MySQL 数据库服务（3306端口）、Spring Boot 后端服务（8080端口）、Vue3 求职者前端（80端口）、Vue2 管理前端（8081端口）。在 docker-compose.yml 中配置了服务依赖关系，backend 服务会等待 mysql 服务通过健康检查后才启动。如果 MySQL 容器异常重启，由于配置了 restart: always 策略，容器会自动重启恢复，后端服务在数据库恢复后通过连接池重试机制即可重新建立连接。（回答正确）

5.你的系统支持"导游证"等行业资质证书的结构化录入，这在通用招聘平台中很少见。请说明这个字段在数据库中是如何设计的，以及如何在职位搜索时利用该字段进行精准筛选？
答：这个字段我是直接存在用户表的 certificate 字段里的，类型是 VARCHAR，里面存证书名称。职位搜索的时候，我通过 SQL 的 LIKE 模糊查询来匹配，比如 WHERE certificate LIKE '%导游证%'，这样就能筛选出有对应证书的用户了。（基本正确）

6.系统的简历模块支持工作经历和教育经历的多条记录管理，请问你在数据库层面是如何处理这种"一对多"关系的，在前端表单中又是如何实现动态增删行的？
答：工作经历和教育经历分别对应 work_experience 和 education_experience 两张独立的表，都通过 resume_id 外键与 resume 简历主表关联，这是一对多的关系。前端表单中使用的是 Element Plus 的 el-form 组件，通过 Vue 3 的响应式数组 ref 来管理动态表单项，用户点击"添加"按钮时向数组 push 一个新的空对象，点击"删除"时用 splice 移除对应索引的项，提交时把整个数组传给后端。（回答正确）
