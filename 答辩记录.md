# 答辩记录

| 项目 | 内容 |
|------|------|
| 论文题目 | 基于 Spring Boot 与 Vue3 的旅游行业招聘系统设计与实现 |
| 答辩人 | ___________ |
| 专业 | 计算机科学与技术 |
| 答辩日期 | 2025年6月 |

---

**1. 你提到系统采用了基于 TF-IDF 的智能岗位推荐算法，请简要说明 TF-IDF 的基本原理，以及在本系统中是如何计算简历与职位之间相似度的？**

答：TF-IDF 是一种常用的文本特征提取方法，TF 是词频，IDF 是逆文档频率。在本系统中，我是先将所有职位描述和简历内容分词，然后分别计算每个词的 TF-IDF 值，生成职位向量和简历向量，最后通过余弦相似度计算两者之间的相似度，返回相似度最高的前 10 个职位推荐给用户。

**评判：回答正确。**

答辩人对 TF-IDF 的基本原理理解准确，能够清晰描述从文本预处理、向量构建到相似度计算的完整流程，体现了对推荐算法模块的深入掌握。

---

**2. 系统权限管理是基于 lin-cms 框架实现的，请说明一下系统中是如何区分求职者、HR 和管理员三类角色的权限的，如果 HR 试图调用管理员接口会发生什么？**

答：系统是通过 JWT Token 来区分角色的，每个用户登录后会拿到一个 token，前端请求时带上这个 token，后端解析 token 就知道是谁了。如果 HR 调用管理员接口，后端会返回 401 未授权错误。

**评判：基本正确。**

答辩人对 JWT 认证流程的描述基本正确，但回答不够完整。lin-cms 框架实际上是基于**分组（Group）+ 权限注解**的方式实现权限控制的：通过 `@GroupRequired` 和 `@PermissionMeta` 注解在接口层面标记所需权限组，而非仅靠 JWT Token 解析角色。当 HR（Group 2）调用管理员（Group 1）接口时，框架会在权限校验阶段拦截并返回 403 Forbidden 权限不足错误，而非 401 未认证。此外，lin-cms 还支持 `@Logger` 操作日志记录，该部分答辩人未提及。

---

**3. 系统前端使用了 Vue 3 + Vite 的求职者端和 Vue 2 的管理端两个版本，你为什么没有统一使用 Vue 3？这样做会不会增加维护成本？**

答：因为我先写管理端的时候还不会 Vue 3，所以用了 Vue 2，后来做求职者端的时候学了 Vue 3，觉得 Vue 3 更好用就用了 Vue 3。这样做确实会增加维护成本，但两个端功能不一样，分开维护也可以接受。

**评判：错误。**

答辩人未能准确回答技术选型原因。根据论文内容，管理端选用 Vue 2 + Element UI 是基于**项目复用和稳定性**考虑：lin-cms 框架本身提供了 Vue 2 版本的管理后台模板，直接复用可以显著减少开发工作量；Element UI 在 Vue 2 生态中经过多年迭代已非常稳定。而求职者端采用 Vue 3 + Vite + Element Plus 是为了利用 Composition API 更好的逻辑复用性、TypeScript 支持以及 Vite 更快的构建速度，属于面向用户侧的体验优化。回答中"先写管理端不会 Vue 3"属于开发时序颠倒且不符合论文描述的技术选型逻辑。

---

**4. 请描述一下系统的 Docker Compose 部署架构，如果 MySQL 容器因异常重启，后端服务会如何处理？**

答：系统通过 Docker Compose 部署了四个服务：MySQL 数据库服务（3306端口）、Spring Boot 后端服务（8080端口）、Vue3 求职者前端（80端口）、Vue2 管理前端（8081端口）。在 docker-compose.yml 中配置了服务依赖关系，backend 服务会等待 mysql 服务通过健康检查后才启动。如果 MySQL 容器异常重启，由于配置了 `restart: always` 策略，容器会自动重启恢复，后端服务在数据库恢复后通过连接池重试机制即可重新建立连接。

**评判：回答正确。**

答辩人对四服务容器化架构描述清晰，准确指出了服务间的启动依赖关系（backend 依赖 mysql 健康检查），也说明了 Docker 自动重启策略和数据库连接池的容错机制。此外还隐含体现了对生产环境可用性设计的理解。

---

**5. 你的系统支持"导游证"等行业资质证书的结构化录入，这在通用招聘平台中很少见。请说明这个字段在数据库中是如何设计的，以及如何在职位搜索时利用该字段进行精准筛选？**

答：这个字段我是直接存在用户表的 `certificate` 字段里的，类型是 VARCHAR，里面存证书名称。职位搜索的时候，我通过 SQL 的 LIKE 模糊查询来匹配，比如 `WHERE certificate LIKE '%导游证%'` ，这样就能筛选出有对应证书的用户了。

**评判：基本正确。**

答辩人对证书字段的存储方式描述有误。根据系统数据库设计，行业资质证书采用的是**独立表结构**设计，即 `certificate` 表与 `user` 表通过外键关联，支持一个用户拥有多个证书（一对多关系），而非直接存储在用户表的单个 VARCHAR 字段中。职位搜索时的筛选逻辑也更为完善：系统通过职位要求的 `required_certificate` 字段与求职者的证书列表进行匹配，同时结合 TF-IDF 语义相似度进行综合排序，而非仅靠 LIKE 模糊查询。答辩人将多对多关联关系简化为了单字段存储，且忽略了语义匹配在精准筛选中的作用。

---

**6. 系统的简历模块支持工作经历和教育经历的多条记录管理，请问你在数据库层面是如何处理这种"一对多"关系的，在前端表单中又是如何实现动态增删行的？**

答：工作经历和教育经历分别对应 `work_experience` 和 `education_experience` 两张独立的表，都通过 `resume_id` 外键与 `resume` 简历主表关联，这是一对多的关系。前端表单中使用的是 Element Plus 的 `el-form` 组件，通过 Vue 3 的响应式数组 `ref` 来管理动态表单项，用户点击"添加"按钮时向数组 push 一个新的空对象，点击"删除"时用 `splice` 移除对应索引的项，提交时把整个数组传给后端。

**评判：回答正确。**

答辩人对一对多关系的数据库设计描述准确，前端动态表单实现方式也符合 Vue 3 + Element Plus 的技术方案，能够正确使用响应式数组操作实现增删功能，体现了前后端联动的完整思考。

---

## 答辩综合评价

该答辩人对系统的核心技术栈（Spring Boot、Vue 3、Docker Compose）掌握较好，能够清晰描述智能推荐算法的实现原理和容器化部署架构。但在权限控制的具体机制、数据库表结构设计的细节上存在概念性偏差，将多表关联关系简化为单字段存储，且对技术选型理由的阐述缺乏论文依据。总体表现为对系统整体架构理解到位，但对部分底层实现细节记忆不够精确。
